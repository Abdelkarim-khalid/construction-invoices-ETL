"""
ملف لإنشاء قاعدة البيانات للمرة الأولى فقط
هذا الملف يُشغل مرة واحدة فقط عند البداية
بعد ذلك استخدم Alembic migrations للتغييرات
"""
from database import engine, Base
import models

def init_database():
    """إنشاء جميع الجداول في قاعدة البيانات"""
    print("🚀 بدء إنشاء قاعدة البيانات...")
    Base.metadata.create_all(bind=engine)
    print("✅ تم إنشاء قاعدة البيانات بنجاح!")
    print("⚠️ احذف أو اعمل comment لهذا الملف بعد التشغيل الأول")

if __name__ == "__main__":
    init_database()
